name: Build RPM Package (Rocky Linux 8 ARM64)

on:
  workflow_dispatch:
    inputs:
      miniforge_version:
        description: 'Miniforge version (e.g., 24.7.1-0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-rpm:
    runs-on: ubuntu-24.04-arm
    container: rockylinux:8

    steps:
      - name: Install build dependencies
        run: |
          dnf -y install dnf-plugins-core rpm-build rpmdevtools tar gzip wget which sed gcc gcc-c++ make

      - name: Cache build dependencies
        uses: actions/cache@v4
        with:
          path: |
            /var/cache/dnf
            /var/cache/yum
          key: build-deps-rocky8-arm64
          retention-days: 30

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Miniforge (ARM64)
        run: |
          MINIFORGE_VERSION=${{ inputs.miniforge_version }}
          wget -q "https://github.com/conda-forge/miniforge/releases/download/${MINIFORGE_VERSION}/Miniforge3-${MINIFORGE_VERSION}-Linux-aarch64.sh" -O miniforge.sh

      - name: Install Miniforge
        run: |
          chmod +x miniforge.sh
          ./miniforge.sh -b -p /usr/local/miniforge3
          rm miniforge.sh

      - name: Verify installation
        run: |
          /usr/local/miniforge3/bin/python --version

      - name: Install Python dependencies from requirements.txt
        run: |
          /usr/local/miniforge3/bin/pip install --upgrade pip setuptools wheel
          /usr/local/miniforge3/bin/pip install --prefer-binary -r requirements.txt

      - name: Get Python version
        run: |
          /usr/local/miniforge3/bin/python -c "import sys; print(sys.version.split()[0])" > /tmp/pyver.txt

      - name: Get glibc version
        run: |
          ldd --version | head -1 | sed 's/.*)//' | sed 's/)//' > /tmp/glibc.txt

      - name: Prepare RPM build environment
        run: |
          mkdir -p /github/home/.rpmbuild/{BUILD,RPMS,SOURCES,SPECS,SRPMS,BUILDROOT}

      - name: Create source tarball
        env:
          MINIFORGE_VERSION: ${{ inputs.miniforge_version }}
        run: |
          RPM_VERSION=$(echo "$MINIFORGE_VERSION" | tr '-' '_')
          cd /usr/local
          tar czf /github/home/.rpmbuild/SOURCES/miniforge3-${RPM_VERSION}.tar.gz miniforge3

      - name: Prepare spec file
        env:
          MINIFORGE_VERSION: ${{ inputs.miniforge_version }}
        run: |
          RPM_VERSION=$(echo "$MINIFORGE_VERSION" | tr '-' '_')
          sed "s/@VERSION@/$RPM_VERSION/" miniforge3.spec > /github/home/.rpmbuild/SPECS/miniforge3.spec

      - name: Build RPM
        env:
          MINIFORGE_VERSION: ${{ inputs.miniforge_version }}
        run: |
          RPM_VERSION=$(echo "$MINIFORGE_VERSION" | tr '-' '_')
          TOPDIR=/github/home/.rpmbuild
          rpmbuild -bb $TOPDIR/SPECS/miniforge3.spec --define "_topdir $TOPDIR" --target aarch64

      - name: List RPM files
        run: |
          find /github/home/.rpmbuild -name "*.rpm" -type f

      - name: Upload RPM artifact
        uses: actions/upload-artifact@v4
        with:
          name: miniforge3-rpm-rocky8-arm64-${{ inputs.miniforge_version }}
          path: /github/home/.rpmbuild/RPMS/**/*.rpm
          retention-days: 5

      - name: Get pip freeze
        run: |
          /usr/local/miniforge3/bin/pip freeze > /tmp/pip_freeze.txt

      - name: Create package table
        run: |
          echo '| Package | Version |' > /tmp/tbl.md
          echo '|---------|---------|' >> /tmp/tbl.md
          grep -v '^#' /tmp/pip_freeze.txt | grep -v '^$' | grep -v '@ file://' | grep '==' | while read line; do
            echo "| $line |" | sed 's/==/|/g' >> /tmp/tbl.md
          done

      - name: Create release body
        run: |
          VERSION="${{ inputs.miniforge_version }}"
          RPM_VERSION=$(echo "$VERSION" | tr '-' '_')
          PYVER=$(cat /tmp/pyver.txt)
          GLIBC=$(cat /tmp/glibc.txt)

          {
            echo "# Miniforge3 RPM (Rocky Linux 8 ARM64) ${VERSION}"
            echo ""
            echo "## Environment"
            echo "- Miniforge: ${VERSION}"
            echo "- Python: ${PYVER}"
            echo "- glibc: ${GLIBC}"
            echo "- Installation: /usr/local/miniforge3"
            echo "- Platform: Rocky Linux 8 aarch64"
            echo ""
            echo "## Installed Packages"
            echo ""
            cat /tmp/tbl.md
            echo ""
            echo "## Installation"
            echo '```'
            echo "sudo rpm -ivh miniforge3-${RPM_VERSION}-1.el8.aarch64.rpm"
            echo '```'
            echo ""
            echo '```'
            echo "sudo dnf localinstall miniforge3-${RPM_VERSION}-1.el8.aarch64.rpm"
            echo '```'
          } > /tmp/release_body.txt

      - name: Upload to Release
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: rpm-rocky8-arm64-${{ inputs.miniforge_version }}
          release_name: Miniforge3 RPM (Rocky Linux 8 ARM64) ${{ inputs.miniforge_version }}
          files: |
            /github/home/.rpmbuild/RPMS/**/*.rpm
            /github/home/.rpmbuild/SOURCES/*.tar.gz
          body_path: /tmp/release_body.txt